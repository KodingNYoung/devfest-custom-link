export enum StorageKeys {
  SESSION_ID = "@user.id",
}

export enum MessageSenders {
  CUSTOMER = "customer",
  SATE = "sate",
  AGENT = "agent",
}

export enum TicketStatus {
  OPEN = "open",
  CLOSED = "closed",
}

export enum FileExtensions {
  DOCX = ".docx",
  DOC = ".doc",
  GIF = ".gif",
  JPG = ".jpg",
  JPEG = ".jpeg",
  MP3 = ".mp3",
  PDF = ".pdf",
  PNG = ".png",
  SVG = ".svg",
  TXT = ".txt",
  XLSX = ".xlsx",
  XLS = ".xls",
}
