export enum StorageKeys {
  SESSION_ID = "@user.id",
}

export enum MessageSenders {
  CUSTOMER = "customer",
  SATE = "sate",
  AGENT = "agent",
}

export enum TicketStatus {
  OPEN = "open",
  CLOSED = "closed",
}
